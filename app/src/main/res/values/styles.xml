<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="Theme.DialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="Theme.PageIndicatorDefaultsNoTitleBarFullscreen" parent="android:Theme.NoTitleBar.Fullscreen">
        <item name="vpiIconPageIndicatorStyle">@style/Widget.IconPageIndicator</item>
        <item name="vpiTabPageIndicatorStyle">@style/Widget.TabPageIndicatorHome</item>
    </style>


    <style name="Widget.TabPageIndicatorHome" parent="Widget">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/global_tab_background</item>
        <item name="android:paddingLeft">22dip</item>
        <item name="android:paddingRight">22dip</item>
        <item name="android:textAppearance">@style/TextAppearance.TabPageIndicatorHome</item>
        <item name="android:textSize">25sp</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="TextAppearance.TabPageIndicatorHome" parent="Widget">
        <item name="android:typeface">monospace</item>
        <item name="android:textColor">@color/global_tab_txt_color</item>
    </style>

    <style name="focusAndClick">
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
    </style>

    <style name="app_item" parent="focusAndClick">

    </style>

    <style name="app_item_big" parent="app_item">
        <item name="android:layout_width">@dimen/px262</item>
        <item name="android:layout_height">@dimen/px360</item>
    </style>

    <style name="app_item_small" parent="app_item">
        <item name="android:layout_width">@dimen/px179</item>
        <item name="android:layout_height">@dimen/px179</item>
    </style>

    <style name="news_item" parent="focusAndClick">
        <item name="android:background">@drawable/selector_news_main</item>
    </style>
    <style name="news_item_right" parent="news_item">
        <item name="android:paddingLeft">15dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:layout_height">@dimen/px162</item>
        <item name="android:layout_width">@dimen/px531</item>
    </style>
    <style name="PopupAnimation" parent="android:Animation" mce_bogus="1">
        <item name="android:windowEnterAnimation">@anim/popup_enter</item>
        <item name="android:windowExitAnimation">@anim/popup_exit</item>
    </style>

    <style name="radiobutton">
        <item name="android:button">@null</item>
        <item name="android:textSize">24sp</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_width">100dp</item>
        <item name="android:background">@drawable/menu_radio_selector</item>
        <item name="android:textColor">@drawable/tv_radio_selector</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="roomRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/room_rating_bar</item>
        <item name="android:minHeight">18dp</item>
        <item name="android:maxHeight">18dp</item>
        <item name="android:isIndicator">true</item>
    </style>

    <style name="popupDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>              <!-- 设置title -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>               <!-- 设置边框 -->
        <item name="android:windowIsTranslucent">true</item>        <!-- 设置半透明 -->
        <!--<item name="android:windowFullscreen">true</item>           &lt;!&ndash; 设置全屏 &ndash;&gt;-->
    </style>

    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
